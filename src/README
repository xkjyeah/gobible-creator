1. Checkout source from googlecode
2. Change to 'src' directory
3. Type 'make' (or, to remove the svn version numbers: 'RELEASE=1 make')
4. Produces GoBibleCreator.jar
5. Run with java -jar GoBibleCreator.jar
